//! Mode A/C decoding - stubs for mode_ac.c translation

/// Convert a Mode C code (in 100-ft units) to a Mode A code.
/// Returns `None` if the conversion is invalid.
pub fn mode_c_to_mode_a(mode_c: i32) -> Option<u32> {
    // TODO: accurate translation from mode_ac.c
    // Stub returns None for every input.
    let _ = mode_c;
    None
}
